<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--Detail Info-->
    <style name="DetailInfo">
        <item name="android:textSize">@dimen/infoTextSize</item>
        <item name="android:textColor">@android:color/darker_gray</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_marginTop">@dimen/infoTop</item>
        <item name="android:layout_marginLeft">@dimen/infoLeft</item>
        <item name="android:layout_marginStart">@dimen/infoStart</item>
    </style>

    <!--Detail Overview-->
    <style name="DetailOverview">
        <item name="android:textSize">@dimen/overViewTextSize</item>
        <item name="android:textColor">@android:color/darker_gray</item>
    </style>

    <!--Detail Title-->
    <style name="DetailTitle">
        <item name="android:textSize">@dimen/TitleTextSize</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textStyle">bold</item>
    </style>


</resources>
